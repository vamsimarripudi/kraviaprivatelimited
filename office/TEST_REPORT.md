# KRAVIA Office v2.0 — Final Build Test Report

## Automated result

`13 passed`

The automated suite covers:

1. customer → invoice → payment → receipt → GST working summary;
2. same-state CGST/SGST and inter-state IGST;
3. invoice number length and controlled sequencing;
4. invoice/payment idempotency;
5. overpayment rejection;
6. duplicate external payment reference rejection;
7. safe public invoice verification;
8. invoice and receipt PDF rendering;
9. double-entry journal/trial-balance balancing;
10. Board Meeting → Resolution → CTC → Authority Grant;
11. Vendor / Contract / Employee / Asset / private Document Vault flows;
12. server-side role guards;
13. Plan → Subscription;
14. Credit Note → tax/revenue reversal accounting;
15. Payment Refund → customer-credit/bank accounting;
16. bank-account/transaction import and deterministic payment auto-match;
17. Command Center derived metrics;
18. maker-checker self-approval prevention;
19. Notice Case creation;
20. Inspection Case + locked-document manifest generation;
21. secret-bearing integration configuration rejection and safe secret-reference acceptance.

## Migration validation

- Alembic initial migration upgraded on a clean database.
- v2 commercial/finance/control migration autogenerated from the current SQLAlchemy metadata.
- Clean migration database upgraded to v2 successfully.

## Static/runtime validation

- JavaScript syntax checks: `data.js`, `engine.js`, `app.js`.
- Python backend compile check.
- FastAPI startup and endpoint execution through TestClient.
- PDF render checks verify `%PDF` output.
- SHA-256 evidence generation executed.

## Environmental limitation

Automated browser screenshot/Playwright visual review is not recorded as passed because browser-to-localhost/file navigation is restricted in this execution environment. The UI still requires final staging visual/accessibility/device review before production promotion.

## Mandatory production acceptance tests after real credentials are configured

- OIDC + MFA + all-role authorization matrix
- PostgreSQL concurrency / sequence / failover tests
- Razorpay signed-webhook replay and settlement tests
- Bank-feed reconciliation
- CA-approved tax golden cases
- statutory accounting close/reconciliation
- eSign/DSC immutability
- IDOR/BOLA/CSRF/XSS/injection/file-upload security testing
- malware scanning
- backup restore / PITR / disaster recovery
- staging E2E with real sandbox providers
- inspection-pack dry run against the authoritative production evidence repository
