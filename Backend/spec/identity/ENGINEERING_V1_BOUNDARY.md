# Engineering Control Center V1 boundary

The control center stores service, repository and runtime references, deployment evidence, and incident lifecycle records. It never stores provider credentials. Deployment and provider health must come from trusted integrations or verified imports; empty evidence remains empty rather than being replaced with sample status.
